import type { backendInterface } from "../backend";
import {
  BillingPeriod,
  InvoiceStatus,
  PlanTier,
  SubscriptionStatus,
  TeamMemberStatus,
  TransactionStatus,
  UserRole,
  UserRole__1,
} from "../backend";

const mockPrincipal = { toString: () => "aaaa-bbbb-cccc" } as any;
const now = BigInt(Date.now()) * BigInt(1_000_000);

export const mockBackend: backendInterface = {
  adminSetSubscription: async () => undefined,
  assignCallerUserRole: async () => undefined,
  cancelMySubscription: async () => undefined,
  confirmPayment: async () => ({
    id: "txn-001",
    status: TransactionStatus.succeeded,
    userId: mockPrincipal,
    createdAt: now,
    description: "Pro Plan - Monthly",
    amountCents: BigInt(2900),
    currency: "USD",
    stripePaymentIntentId: "pi_mock_001",
  }),
  createCheckoutSession: async () => "cs_mock_session_id",
  getAllInvoices: async () => [
    {
      id: "inv-001",
      status: InvoiceStatus.paid,
      userId: mockPrincipal,
      createdAt: now,
      description: "Pro Plan - Monthly",
      amountCents: BigInt(2900),
      currency: "USD",
      paidAt: now,
    },
    {
      id: "inv-002",
      status: InvoiceStatus.paid,
      userId: mockPrincipal,
      createdAt: now - BigInt(30 * 24 * 60 * 60 * 1_000_000_000),
      description: "Pro Plan - Monthly",
      amountCents: BigInt(2900),
      currency: "USD",
      paidAt: now - BigInt(30 * 24 * 60 * 60 * 1_000_000_000),
    },
  ],
  getAllTransactions: async () => [
    {
      id: "txn-001",
      status: TransactionStatus.succeeded,
      userId: mockPrincipal,
      createdAt: now,
      description: "Pro Plan - Monthly",
      amountCents: BigInt(2900),
      currency: "USD",
      stripePaymentIntentId: "pi_mock_001",
    },
  ],
  getCallerUserProfile: async () => ({
    billingAddress: undefined,
    userId: mockPrincipal,
    name: "Alex Johnson",
    createdAt: now,
    role: UserRole.customer,
    email: "alex@example.com",
  }),
  getCallerUserRole: async () => UserRole__1.admin,
  getMyInvoice: async () => ({
    id: "inv-001",
    status: InvoiceStatus.paid,
    userId: mockPrincipal,
    createdAt: now,
    description: "Pro Plan - Monthly",
    amountCents: BigInt(2900),
    currency: "USD",
    paidAt: now,
  }),
  getMyInvoices: async () => [
    {
      id: "inv-001",
      status: InvoiceStatus.paid,
      userId: mockPrincipal,
      createdAt: now,
      description: "Pro Plan - Monthly",
      amountCents: BigInt(2900),
      currency: "USD",
      paidAt: now,
    },
  ],
  getMySubscription: async () => ({
    status: SubscriptionStatus.active,
    nextBillingDate: now + BigInt(30 * 24 * 60 * 60 * 1_000_000_000),
    userId: mockPrincipal,
    tier: PlanTier.pro,
    billingPeriod: BillingPeriod.monthly,
    startDate: now - BigInt(30 * 24 * 60 * 60 * 1_000_000_000),
  }),
  getMyTransactions: async () => [
    {
      id: "txn-001",
      status: TransactionStatus.succeeded,
      userId: mockPrincipal,
      createdAt: now,
      description: "Pro Plan - Monthly",
      amountCents: BigInt(2900),
      currency: "USD",
      stripePaymentIntentId: "pi_mock_001",
    },
  ],
  getMyUsage: async () => ({
    month: BigInt(5),
    userId: mockPrincipal,
    year: BigInt(2026),
    apiCallCount: BigInt(3842),
  }),
  getPlans: async () => [
    {
      features: ["5 API Calls/month", "Community Support", "Basic Analytics"],
      annualPriceCents: BigInt(0),
      monthlyPriceCents: BigInt(0),
      name: "Free",
      tier: PlanTier.free,
      description: "Perfect for getting started",
      apiCallLimit: BigInt(5),
    },
    {
      features: ["10,000 API Calls/month", "Priority Support", "Advanced Analytics", "Team Members (up to 5)"],
      annualPriceCents: BigInt(29000),
      monthlyPriceCents: BigInt(2900),
      name: "Pro",
      tier: PlanTier.pro,
      description: "For growing teams",
      apiCallLimit: BigInt(10000),
    },
    {
      features: ["Unlimited API Calls", "24/7 Dedicated Support", "Custom Analytics", "Unlimited Team Members", "SLA Guarantee"],
      annualPriceCents: BigInt(99000),
      monthlyPriceCents: BigInt(9900),
      name: "Enterprise",
      tier: PlanTier.enterprise,
      description: "For large organizations",
      apiCallLimit: BigInt(0),
    },
  ],
  getStripeSessionStatus: async () => ({
    __kind__: "failed",
    failed: { error: "Mock: Stripe not configured" },
  }),
  getTeamMembers: async () => [
    {
      status: TeamMemberStatus.active,
      joinDate: now - BigInt(60 * 24 * 60 * 60 * 1_000_000_000),
      name: "Sarah Chen",
      role: UserRole.manager,
      invitedBy: mockPrincipal,
      email: "sarah@example.com",
      principalId: mockPrincipal,
    },
    {
      status: TeamMemberStatus.active,
      joinDate: now - BigInt(45 * 24 * 60 * 60 * 1_000_000_000),
      name: "James Park",
      role: UserRole.customer,
      invitedBy: mockPrincipal,
      email: "james@example.com",
      principalId: mockPrincipal,
    },
  ],
  getUserProfile: async () => ({
    billingAddress: undefined,
    userId: mockPrincipal,
    name: "Alex Johnson",
    createdAt: now,
    role: UserRole.admin,
    email: "alex@example.com",
  }),
  getUserSubscription: async () => ({
    status: SubscriptionStatus.active,
    nextBillingDate: now + BigInt(30 * 24 * 60 * 60 * 1_000_000_000),
    userId: mockPrincipal,
    tier: PlanTier.pro,
    billingPeriod: BillingPeriod.monthly,
    startDate: now - BigInt(30 * 24 * 60 * 60 * 1_000_000_000),
  }),
  getUserUsage: async () => ({
    month: BigInt(5),
    userId: mockPrincipal,
    year: BigInt(2026),
    apiCallCount: BigInt(3842),
  }),
  inviteTeamMember: async () => undefined,
  isCallerAdmin: async () => true,
  isStripeConfigured: async () => false,
  listAllUsers: async () => [
    {
      billingAddress: undefined,
      userId: mockPrincipal,
      name: "Alex Johnson",
      createdAt: now,
      role: UserRole.admin,
      email: "alex@example.com",
    },
    {
      billingAddress: undefined,
      userId: mockPrincipal,
      name: "Sarah Chen",
      createdAt: now - BigInt(60 * 24 * 60 * 60 * 1_000_000_000),
      role: UserRole.manager,
      email: "sarah@example.com",
    },
    {
      billingAddress: undefined,
      userId: mockPrincipal,
      name: "James Park",
      createdAt: now - BigInt(45 * 24 * 60 * 60 * 1_000_000_000),
      role: UserRole.customer,
      email: "james@example.com",
    },
  ],
  recordApiCall: async () => undefined,
  removeTeamMember: async () => undefined,
  saveCallerUserProfile: async () => undefined,
  setStripeConfiguration: async () => undefined,
  setUserRole: async () => undefined,
  transform: async (input) => ({ status: BigInt(200), body: input.response.body, headers: [] }),
  updateTeamMemberStatus: async () => undefined,
  _initializeAccessControl: () => Promise.resolve(undefined),
};
